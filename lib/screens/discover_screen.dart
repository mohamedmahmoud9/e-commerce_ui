import 'package:flutter/material.dart';
class DiscoverScreen extends StatelessWidget {
  static final routeName = 'discover-screen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child:Text('Discover')),
    );
  }
}